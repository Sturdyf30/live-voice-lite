[project]
name = "live-voice-lite"
version = "0.1.0"
description = "Self-hosted local voice interface for Hermes Agent"
requires-python = ">=3.11"

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
